#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ROOT_DIR="$(dirname "$SCRIPT_DIR")"

set -a
. "$ROOT_DIR/.env"
set +a

psql -h "$PGHOST" -p "$PGPORT" -U "$PGUSER" -d "$PGDB" -v schema="${WATER_SCHEMA:-water}" -a -f "$ROOT_DIR/proccess_data.sql"
